# Daily Dashboard

Runs automatically on startup. Shows today's focus, a random task, and tracks your streak.

## Tech Used
- Python
- datetime, random modules
- Windows Task Scheduler

## How to Run
1. Clone the repo
2. Run: `python dashboard.py`
3. To run on startup: add to Windows Startup folder
